#!/usr/bin/env bash
set -e

if [ -n "$WWWUSER" ]; then
    usermod -u "$WWWUSER" scback
fi

gosu scback git config --global --add safe.directory /var/www/html 2>/dev/null || true

exec gosu scback "$@"